import java.lang.Math; //math library

public class stochastic
{
  public static void main(String[] args)
  {
    /* no need to modify this section */
    double a = Double.parseDouble(args[0]);
    double b = Double.parseDouble(args[1]);

    double randDecimal = Math.random();

    /* TODO: write a function to print a random number in range [a,b)
    using an equation involving randDecimal, a, and b  */

  }
}
